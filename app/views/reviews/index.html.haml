-title "Reviews for #{@tour || @performer}"
%h1.mb-2
  Reviews for
  =link_to_self(@tour || @performer)
.well.mt1
  %b.icon-info-sign
  Your first-hand experiences really help other promoters.
  =link_to("Click to add your own review.", @tour ? new_tour_review_path(@tour) : new_performer_review_path(@performer))
=render 'reviews/reviews', :reviews => @reviews
=options_panel do
  =link_to "#{content_tag(:i, nil, :class => 'icon-arrow-left')}Back".html_safe, :back, :class => "btn black-block-links"