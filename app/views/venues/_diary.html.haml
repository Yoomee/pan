-if dates.count == 0
  There are no booked dates yet
-else  

  .span
    %table#diary
      %thead
      %tbody
        -dates.each do |date|
          %tr
            %td.date
              =date.date.to_s(:date)
            %td
              =link_to_self date.tour, :class => "venue"
            %td
              =format_time(date.time)
  .clearfix
