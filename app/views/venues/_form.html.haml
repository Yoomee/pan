=semantic_form_for(venue, :html => {:multipart => true}) do |form|
  =tabs('details', 'links', ['contact_info', "Address & Contact Info"], :form => form)
  =form.buttons do
    =form.commit_button "Save"