=semantic_form_for(promoter, :html => {:multipart => true}) do |form|
  =tabs('details', ['resources', 'Skills & Resources'], 'organisations/tabs/links', 'contact_info', ["slideshow", "Photo gallery"], :form => form)
  =form.buttons do
    =form.commit_button "Save"