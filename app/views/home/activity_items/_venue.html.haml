=link_to item, :class=>"news-feed-image" do
  =item.image.present? ? image_for(item, '70x70#') : image_tag('placeholder-organisation.jpg')
.news-feed-text  
  %p
    -if item.promoter.present?
      =link_to item.promoter, promoter_path(item.promoter)
      added a new venue.
    -else
      New venue
  %h3
    =link_to_self item