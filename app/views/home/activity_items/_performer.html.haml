=link_to item, :class=>"news-feed-image" do
  =item.image.present? ? image_for(item, '70x70#') : image_tag('placeholder-performer.jpg')
.news-feed-text  
  %p
    A new performer was added 
  %h3
    =link_to_self item